const Setting = {
  debounceInterval: 100,
  draftInterval: 1000 * 10,
  maxStore: 10,
  maxClipBoard: 20,
  maxScale: 2.0,
  minScale: 0.5,
  staticComponents: [
    'media', 'text'
  ],
  componentSetComponents: [
    'carousel-container', 'tab-container'
  ]
}

export default Setting
