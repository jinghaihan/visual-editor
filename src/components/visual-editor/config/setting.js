const Setting = {
  debounceWait: 100,
  storeWaitTime: 1000 * 10,
  maxStore: 10,
  maxClipBoard: 20,
  maxScale: 2.0,
  minScale: 0.5,
  staticComponents: [
    'media', 'text'
  ]
}

export default Setting
